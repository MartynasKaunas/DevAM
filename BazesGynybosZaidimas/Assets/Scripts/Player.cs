using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player : MonoBehaviour {
	public int PlayerHealth = 100; // player health points.
	public bool isDead = false; // bool to check if player is dead.
	public bool damageTaken = false; // bool to check if player got damaged.


	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {

	}
}
